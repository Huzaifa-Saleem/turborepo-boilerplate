/**
 * Server-specific types
 * For shared types, use @repo/types instead
 */

// Re-export shared types for convenience
export type {
  ApiResponse,
  PaginatedResponse,
  ApiError,
  HealthCheckResponse,
} from "@repo/types";
